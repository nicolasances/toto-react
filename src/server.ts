/**
 * toto-react — server-side barrel
 *
 * Re-export all Next.js API route handlers here so consuming apps can
 * re-export them from their own `app/api/` routes.
 * Example:
 *   export { STTHandler } from './api/stt';
 *   export { TTSHandler } from './api/tts';
 */
