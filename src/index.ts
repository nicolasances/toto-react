/**
 * toto-react — client-side barrel
 *
 * Re-export all client-side components, hooks, contexts, and API clients here.
 * Example:
 *   export { MyComponent } from './components/MyComponent';
 *   export { useMyHook } from './hooks/useMyHook';
 */
